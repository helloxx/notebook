git init	#把目录变为git可以管理的仓库
git add <file>		#提交文件到暂存区
git commit -m (message)	#提交暂存区的修改到工作区
git status	#查看仓库当前状态
git diff	#查看未提交的更改差异
git log		#显示最近到最远的提交日志
	--pretty=oneline	#每次提交显示为一行
git reset --hard HEAD^	#回到上次版本
	gir reset --hard commit_id	#回到对应commit_id的版本
git reflog	#查看你的每次命令
	

